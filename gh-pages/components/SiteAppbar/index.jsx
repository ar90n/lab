import React from 'react'
import { prefixLink } from 'gatsby-helpers'
import { config } from 'config'
import { AppBar } from 'material-ui'

const SiteAppbar = props => <AppBar {...props} />

export default SiteAppbar
