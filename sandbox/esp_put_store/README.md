# http put storage

Simple http storage.

## setup
```
$ pip install -U platformio
```

## build
Rename `secrets.h.tempalte` to `secrets.h` and fill in correct SSID and PASS.
And then run following.

```
$ pio run -t upload
```